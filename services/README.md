# services

This directory contains shared service modules (e.g. database clients, AI clients, caching helpers).

The main router is exported from `index.js` and mounted at `/services` in `server.js`.
