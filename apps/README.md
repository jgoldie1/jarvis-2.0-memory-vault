# apps

This directory contains the application feature modules for Jarvis 2.0 Memory Vault.

Each sub-directory or file here represents a distinct feature area (e.g. `memory.js`, `marketplace.js`).
The main router is exported from `index.js` and mounted at `/apps` in `server.js`.
